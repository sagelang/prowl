// Greatest common divisor via Euclid's algorithm.
// Returns gcd(48, 18) = 6 as the exit code.

fn gcd(a: Int, b: Int) -> Int {
    if b == 0 {
        return a;
    }
    return gcd(b, a % b);
}

fn main() -> Int {
    return gcd(48, 18);
}
